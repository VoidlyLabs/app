import TopBar from '@/shared/ui/top-bar/top-bar';
import ConfigProvider from '@/app/providers/config/config.provider';

export default function Home() {
  return (
    <div className="">
      <TopBar />
    </div>
  );
}
